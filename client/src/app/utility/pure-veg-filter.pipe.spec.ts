import { PureVegFilterPipe } from './pure-veg-filter.pipe';

describe('PureVegFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PureVegFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
