import { UpperCaseConverterButtonPipe } from './upper-case-converter-button.pipe';

describe('UpperCaseConverterButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseConverterButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
